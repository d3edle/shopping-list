{
  "name": "phonebackend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../real_shopping_list && npm run build && cp -r dist ../shopping_list_backend/ && cd ../shopping_list_backend && npm run dev",
    "deploy:full": "rm -rf dist && cd ../phonebook && npm run build && cp -r dist ../phoneBackend/ && cd ../phoneBackend && git add . && git commit -m uibuild && git push",
    "lint": "eslint ."
  },
  "author": "d3edle",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "mongodb": "^6.17.0",
    "mongoose": "^8.16.4",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.32.0",
    "@stylistic/eslint-plugin-js": "^4.4.1",
    "eslint": "^9.32.0",
    "eslint-plugin-react": "^7.37.5",
    "globals": "^16.3.0"
  }
}
